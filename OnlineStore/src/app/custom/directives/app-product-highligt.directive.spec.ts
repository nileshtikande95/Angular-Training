import { AppProductHighligtDirective } from './app-product-highligt.directive';

describe('AppProductHighligtDirective', () => {
  it('should create an instance', () => {
    const directive = new AppProductHighligtDirective();
    expect(directive).toBeTruthy();
  });
});
